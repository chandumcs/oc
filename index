<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Olive 24x7 — Transactional Intelligence</title>

  <!-- Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&family=Poppins:wght@600;700&display=swap" rel="stylesheet">

  <style>
    :root{
      --brand-blue: #112B68;
      --brand-orange: #FF6A00;
      --mint-accent: #A6FFD1;
      --bg: #F7F9FB;
      --surface: #FFFFFF;
      --muted: #6B7280;
      --text: #0F1724;

      --radius-sm: 8px;
      --radius-md: 14px;
      --shadow-lg: 0 12px 30px rgba(16,24,40,0.08);

      --space-xs: .25rem;
      --space-sm: .5rem;
      --space-md: 1rem;
      --space-lg: 1.5rem;
      --space-xl: 2.5rem;
    }

    /* Reset / base */
    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0;
      font-family: "Inter", system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
      background:var(--bg);
      color:var(--text);
      -webkit-font-smoothing:antialiased;
      -moz-osx-font-smoothing:grayscale;
    }

    a{color:inherit;text-decoration:none}

    /* Container */
    .container{max-width:1200px;margin:0 auto;padding:0 var(--space-lg)}

    /* NAVBAR */
    .nav{
      height:72px;
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap:16px;
    }
    .nav .left{display:flex;align-items:center;gap:18px}
    .logo{height:44px}
    .nav .menu{display:flex;gap:22px;align-items:center;font-weight:500;color:var(--muted)}
    .nav .menu a{padding:8px 6px}
    .nav .cta{background:var(--mint-accent);color:var(--brand-blue);padding:10px 18px;border-radius:12px;font-weight:700;box-shadow:var(--shadow-lg);border:0;cursor:pointer}
    .nav .icon-btn{width:44px;height:44px;display:inline-grid;place-items:center;border-radius:10px;background:transparent;border:1px solid rgba(16,24,40,0.04)}

    /* HERO */
    .hero{
      display:grid;
      grid-template-columns: 1fr 520px;
      gap:40px;
      align-items:center;
      padding:40px 0 80px;
    }

    .hero-left{
      position:relative;
      padding-top:20px;
    }

    .decor-rect{
      position:absolute;
      left:-40px;
      top:0;
      width:44px;
      height:720px;
      background:linear-gradient(180deg,var(--mint-accent),rgba(166,255,209,0.5));
      border-radius:6px;
      filter:blur(0.4px);
      opacity:0.95;
      z-index:0;
    }

    .hero-title{
      font-family:"Poppins", sans-serif;
      font-size:64px;
      line-height:0.98;
      margin:0 0 18px;
      color:var(--brand-blue);
      z-index:1;
    }
    .hero-sub{
      color:var(--muted);
      font-size:18px;
      max-width:640px;
      margin-bottom:26px;
      z-index:1;
    }

    .email-cta{
      display:flex;
      gap:8px;
      max-width:560px;
      align-items:center;
      z-index:1;
    }
    .email-cta input{
      flex:1;
      height:56px;
      border-radius:10px;
      border:1px solid rgba(16,24,40,0.08);
      padding:0 16px;
      font-size:16px;
      background:var(--surface);
      box-shadow: 0 6px 18px rgba(16,24,40,0.02);
    }
    .email-cta button{
      width:64px;height:56px;border-radius:10px;border:0;background:var(--brand-blue);
      color:#fff;display:grid;place-items:center;font-weight:700;cursor:pointer;
      box-shadow: 0 6px 18px rgba(17,43,104,0.12);
    }
    .privacy{font-size:13px;color:var(--muted);margin-top:8px}

    /* Right visual */
    .hero-right{
      position:relative;
      height:420px;
      display:flex;
      align-items:center;
      justify-content:center;
    }
    .card-stack{
      width:360px;height:260px;border-radius:18px;background:linear-gradient(120deg,#fff,#f8fffd);
      box-shadow:var(--shadow-lg);
      transform:rotate(-12deg);
      position:absolute;right:20px;top:20px;
      display:flex;align-items:center;justify-content:center;
    }
    .card-stack .chip{
      position:absolute;left:28px;top:28px;width:60px;height:34px;background:var(--mint-accent);border-radius:8px;box-shadow:0 6px 18px rgba(10,60,20,0.06)
    }
    .card-stack.secondary{
      transform:rotate(14deg);right:-40px;top:80px;background:linear-gradient(180deg,var(--mint-accent),#EFFFF3);width:420px;height:270px;opacity:0.98;border-radius:20px;
    }

    /* small stat row */
    .stats{display:flex;gap:22px;margin-top:28px}
    .stat{background:var(--surface);padding:12px 18px;border-radius:12px;box-shadow:0 6px 20px rgba(15,23,36,0.03);min-width:120px}
    .stat strong{display:block;font-size:20px;color:var(--brand-blue)}
    .stat span{display:block;font-size:13px;color:var(--muted)}

    /* FOOTER */
    footer{padding:48px 0;border-top:1px solid rgba(16,24,40,0.04);margin-top:40px;background:transparent}
    .footer-grid{display:grid;grid-template-columns:1fr 1fr 1fr 1fr;gap:18px}
    .footer-grid h4{margin:0 0 12px;color:var(--brand-blue);font-size:15px}
    .footer-grid a{display:block;color:var(--muted);font-size:14px;padding:6px 0}

    /* Responsive */
    @media (max-width:1000px){
      .hero{grid-template-columns:1fr 420px}
      .hero-title{font-size:54px}
      .card-stack{right:0;top:40px}
    }
    @media (max-width:820px){
      .nav .menu{display:none}
      .container{padding:0 18px}
      .hero{grid-template-columns:1fr;gap:26px;padding-top:20px}
      .decor-rect{display:none}
      .hero-title{font-size:40px}
      .hero-right{order:2;height:320px}
      .hero-left{order:1}
      .footer-grid{grid-template-columns:1fr 1fr}
    }
    @media (max-width:420px){
      .hero-title{font-size:30px}
      .email-cta input{height:48px}
      .email-cta button{height:48px;width:48px}
      .nav{height:56px}
      .logo{height:36px}
    }
  </style>
</head>
<body>

  <header class="container">
    <nav class="nav">
      <div class="left">
        <!-- Replace src with your logo path -->
        <img src="assets/logo.png" alt="Olive 24x7" class="logo" />
      </div>

      <div class="menu" aria-hidden="true">
        <a href="#">Demos</a>
        <a href="#">About Us</a>
        <a href="#">Portfolio</a>
        <a href="#">Shop</a>
        <a href="#">All Pages</a>
      </div>

      <div class="right" style="display:flex;align-items:center;gap:12px">
        <div class="icon-btn" title="Cart">🛒</div>
        <button class="cta">Get Started</button>
      </div>
    </nav>
  </header>

  <main class="container">
    <section class="hero">
      <div class="hero-left">
        <div class="decor-rect" aria-hidden="true"></div>

        <h1 class="hero-title">Smarter Transactional Intelligence, 24×7</h1>

        <p class="hero-sub">Automating transaction monitoring and real-time insights for banks, payment processors, and fintech platforms. Faster alerts, deeper analytics, and cleaner reconciliation.</p>

        <div class="email-cta" role="form" aria-label="Subscribe form">
          <input type="email" placeholder="Enter your email address" aria-label="Email address" />
          <button aria-label="Subscribe">→</button>
        </div>
        <p class="privacy">We care about your data — read our <a href="#">privacy policy</a>.</p>

        <div class="stats" aria-hidden="true">
          <div class="stat"><strong>24×7</strong><span>Monitoring uptime</span></div>
          <div class="stat"><strong>99.9%</strong><span>Alert accuracy</span></div>
          <div class="stat"><strong>200+</strong><span>Bank integrations</span></div>
        </div>
      </div>

      <div class="hero-right" aria-hidden="true">
        <!-- Visual mockups (use real images if available) -->
        <div class="card-stack secondary"></div>
        <div class="card-stack"></div>
      </div>
    </section>
  </main>

  <footer class="container">
    <div style="max-width:1200px;margin:0 auto;">
      <div style="display:flex;justify-content:space-between;align-items:flex-start;gap:20px;flex-wrap:wrap">
        <div style="min-width:220px">
          <img src="assets/logo.png" alt="Olive 24x7" style="height:46px"/>
          <p style="color:var(--muted);max-width:320px;margin-top:12px">Olive 24x7 provides automated transaction intelligence and monitoring for financial institutions.</p>
        </div>

        <div style="flex:1;min-width:160px">
          <h4>Product</h4>
          <a href="#">Features</a>
          <a href="#">Pricing</a>
          <a href="#">Integrations</a>
        </div>

        <div style="flex:1;min-width:160px">
          <h4>Company</h4>
          <a href="#">About</a>
          <a href="#">Careers</a>
          <a href="#">Contact</a>
        </div>

        <div style="flex:1;min-width:160px">
          <h4>Support</h4>
          <a href="#">Docs</a>
          <a href="#">Status</a>
          <a href="#">Privacy</a>
        </div>
      </div>

      <div style="margin-top:28px;border-top:1px solid rgba(16,24,40,0.04);padding-top:18px;color:var(--muted);font-size:13px;display:flex;justify-content:space-between;flex-wrap:wrap;gap:12px">
        <div>© <strong>Olive 24x7</strong> — Transactional Intelligence</div>
        <div>Made with ❤️</div>
      </div>
    </div>
  </footer>

</body>
</html>
